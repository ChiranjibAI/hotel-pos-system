name: packages
description: "Bluetooth and printer support for Hotel POS System"
version: 1.0.0
homepage:
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

  collection: ^1.19.1
  flutter_blue_plus: 1.35.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner: ^2.5.4
  mockito: ^5.4.6

flutter: